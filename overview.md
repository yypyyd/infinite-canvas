# 本轮三处 UI 修复总览

## 图1：个人中心头部去重复（account/page.tsx）

- 删除指标条里与「账户余额」Tab 重复的 **账户余额 ¥ / 个人算力** 两项
- 删除右上角突兀的 **充值余额** 大按钮（顶部导航已有算力和充值入口，Tab 就在下方）
- 指标条收敛为两项：**生成记录 / 加入时间**（md 两列）

## 图2：余额页结算卡与订单分页（account/page.tsx BalanceSection）

- 订单摘要卡改为**金额区在上、支付方式 + 前往支付按钮 `mt-auto` 吸底**，消除中间死区；应付旁增加「省 ¥x」优惠标签，与档位卡呼应
- 「最近充值订单」→「充值订单」，显示总笔数，**每页 5 条 + 小尺寸分页器**（`fetchPaymentOrders` 传 page，接口本就返回 total）

## 图3：模型中心（model-catalog-editor.tsx）

- **排版**：类型/来源合并为一列（彩色类型 Tag + 下方「渠道同步/手动添加」小字）；能力列从长枚举改为精简摘要（`生成+编辑 · 比例 6 · 1K/2K/4K · 4-15 秒 · 参考图 6`），零值不显示
- **多选删除**：表格加行多选，工具栏出现「删除选中（N）」；仍由渠道提供的模型自动跳过并提示（保留原有保护逻辑）
- **同步清理**：「同步渠道模型」发现已不在任何渠道中的模型时，先弹出清单确认——确认后连同计费规则一并删除；点「保留」则只同步能力不删除（防止手动添加未接渠道的模型被静默误删）

## 待验证

- 个人中心各 Tab 头部展示、结算卡在档位行数变化时的吸底对齐、订单翻页与支付回跳刷新
- 模型中心：多选跨页保留、批量删除跳过渠道模型、同步三方向（新增/更新/删除）、手动添加未接渠道模型不被误删
