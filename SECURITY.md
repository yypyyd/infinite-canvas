# 安全政策

## 支持范围

项目仍处于快速开发阶段，只维护当前 `main` 和最新发布版本。历史版本可能不再接收安全修复。

## 报告漏洞

请不要在公开 Issue 中披露尚未修复的漏洞、凭据或用户数据。优先使用 GitHub 的[私密漏洞报告](https://github.com/yypyyd/infinite-canvas/security/advisories/new)；无法使用时，发送邮件至 [1844025705@qq.com](mailto:1844025705@qq.com)，标题注明“安全漏洞”，并提供受影响版本、复现条件、影响和建议修复方式。维护者确认问题并准备修复后，再协调公开时间。

## 部署者责任

- 首次启动必须设置至少 12 位的 `ADMIN_PASSWORD` 和至少 32 位、长期固定的 `JWT_SECRET`。
- 不要直接暴露数据库、Go 内部端口或对象存储凭据；公网部署必须使用 HTTPS。
- 正式环境应使用 PostgreSQL 或 MySQL、持久存储和可恢复备份，不要依赖免费实例的临时 SQLite。
- 上游模型 API Key、SMTP 密码和对象存储密钥只应通过私有配置提供，不得提交到仓库或日志。

## 公开发布前检查

发布维护者应使用 gitleaks 等工具扫描当前树和全部 Git 历史，并检查已经删除的文件名。仅从最新提交删除敏感文件不会清除历史副本；如果历史中曾包含生产运维目标或凭据，应在公开前建立干净历史，已经公开的凭据必须立即轮换。
