# 个人中心头部与余额区合并调整

## 本轮改动（account/page.tsx）

问题：上一轮把余额/算力从头部拿掉后，指标条只剩"生成记录/加入时间"两项，稀疏空旷；余额区又有一条独立的大色带展示同样数字——两条横带叠放，视觉逻辑冲突。

最终方案——**指标收敛到头部一处**：

- 头部指标条恢复完整四项：**账户余额 / 个人算力 / 生成记录 / 加入时间**
  - 余额、算力两项可点击，分别跳转「账户余额」「算力明细」Tab（hover 有反馈），取代之前突兀的"充值余额"按钮
  - 窄屏 2×2 网格，桌面 4 列分割线
- 余额 Tab 顶部的大色带删除，换成轻量标题行（标题 + 一句话说明 + 分隔线），不再重复展示数字
- 下方依次是：余额兑换单行流程 → 档位 + 订单摘要结算卡 → 分页充值订单

设计规则已沉淀到 AGENTS.md：页面头部已展示的全局指标，内容区不再重复展示大数字。

## 待验证

- 各 Tab 下头部四项指标展示与点击跳转
- 窄屏 2×2 布局、深浅主题
- 余额页从标题行直接阅读到兑换/充值的动线

