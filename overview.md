# 模型渠道页 UI 优化总览

## 本轮（图1 + 图2）

文件：`web/src/app/(admin)/admin/channels/page.tsx`

### 图1：编辑渠道抽屉加宽 + 布局重排

- 宽度 720px → `min(1120px, 100vw - 32px)`，手风琴行和摘要标签不再拥挤
- 表单重排：
  - 第一行：渠道名称 (9) + 协议 (5) + 权重 (5) + 启用 (5)
  - 第二行：接口地址 (12) + API Key (12)
  - 下方依次为模型选择、能力列表、备注
- 窄屏自动换行（xs 断点）

### 图2：选择渠道模型弹窗列表精简

原来每个模型堆 5-8 个 Tag（连"参考视频 0 个""无音频输出"都占位），改为紧凑列表行：

- 一行 = 复选框 + 模型名（加粗，溢出省略带 tooltip）+ 右侧精简摘要（小字灰色）
- 摘要只显示有值的项：`image · 比例 6 · 1K/2K/4K · 4-15 秒 · 参考图 6 · 音频输出`，零值全部省略
- 整行可点击勾选（支持键盘空格/回车），hover 有高亮
- 新增 `discoveredSummary()` helper

## 上一轮（同一会话）

- 渠道模型列表改为手风琴折叠面板 + 行内能力摘要标签 + 搜索过滤
- 账户余额页重排（概览条 / 单行兑换 / 订单摘要结算卡）
- 支付充值设置页对齐 Card 分组规范

## 待验证

- 宽屏/窄屏下抽屉表单换行表现
- 模型选择弹窗：勾选、全选、搜索、摘要准确性、确认后能力回填
- 手风琴面板内编辑与保存回归
