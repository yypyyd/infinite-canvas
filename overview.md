# 本轮 UI 重设计总览

## 一、模型渠道编辑抽屉（本次）

文件：`web/src/app/(admin)/admin/channels/components/channel-model-capabilities-editor.tsx`

**痛点**：渠道挂 28 个模型时，每个模型一张完整配置卡，逐卡滚动查找非常痛苦。

**方案**：改为手风琴折叠列表

- 默认全部收起，每行直接显示：模型名称（对外名 + ID）+ 能力摘要标签（操作 / 分辨率 / 比例数 / 时长数 / 参考图数 / 音频输出）——不展开也能通览全部模型能力
- 点击行展开该模型的完整配置表单；`accordion` 模式一次只展开一个，展开下一个自动收起上一个
- 顶部新增搜索框，按对外名或模型 ID 过滤
- 「移除」按钮保留在每行右侧（已阻止冒泡，不会误触展开）
- 面板项 `forceRender`，收起状态下的必填校验（分辨率、比例等）保存时依然生效
- 每个模型的表单内容原样保留，抽为 `ModelCapabilityFields` 子组件

## 二、账户余额页 & 支付充值设置页（上一轮）

- `account/page.tsx`：余额概览条（余额 + 算力双指标）、单行兑换流程、档位卡片 +「订单摘要」结算卡、优惠档位「省 ¥x」标签
- `payment-settings-editor.tsx`：Card 分组对齐其他设置页、开关移到卡片右上角、两列网格、档位表格拆分独立列

## 待验证

- 渠道抽屉：20+ 模型浏览定位效率、面板内编辑与必填校验、搜索、移除、保存完整性
- 账户余额页 / 支付充值设置页：深浅主题、移动端、支付下单与兑换流程回归
