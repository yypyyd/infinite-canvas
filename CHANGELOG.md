# CHANGELOG

## Unreleased

+ [新增] 对外 API 支持按原 `Idempotency-Key` 查询生成任务的运行、成功或失败状态，恢复图片结果或视频任务编号；个人中心快速接入和模型广场同步提供恢复入口与示例，避免下游断线或超时后重复生成和扣费。

+ [修复] 画布账号数据版本冲突自动恢复，停止过期数据无限重试，生成任务不再因同步冲突误报失败。

+ [修复] 修复 GPT Image 2 提示词分类因原 GitHub 仓库失效返回 404 而为空的问题，改用当前可用的 Freestyle GPT Image 2 数据源。
+ [新增] 提示词管理接入两个新的 GitHub 远程源：Freestyle GPT Image 2（500+ 图像案例）与 Awesome Seedance 2.0（视频提示词案例），可按分类单独同步。

+ [优化] 重做用户端首页结构与动效：保留浅色主题并参考 Krea 当前官网的产品首页层级，加入标题轮播、作品卡漂浮、提示词浮层、能力流动带、场景交互预览和滚动入场，并保留现有画布、任务入口、灵感预览与生成逻辑。
+ [新增] 新增 27 张本地首页作品墙 WebP 素材，来源图片统一压缩到约 30–300KB，首页使用本地懒加载作品流，不依赖七牛；源目录中的完全重复副本不重复写入项目。
+ [优化] 调整首页首屏层级：标题、提示词入口和画布入口独立放少量预览图，下方精选墙承载更多作品；两区拆分使用不同本地素材，每张主展示素材只出现一次，并用轻量上下漂浮与悬停动效避免循环复制造成重复感。
+ [修复] 首页本地作品素材改用带版本的新文件名，替换 WebP 后不会继续命中浏览器旧缓存，且兼容现有 Next Image 本地资源配置。
+ [优化] 精选作品区改为真正的多列瀑布流：按列独立排布全部素材，错开列首位置并混用多种卡片比例，再加入低幅度错位漂浮动效，避免最右列空白或整齐网格感。
+ [修复] 首页明暗主题统一使用全局背景、卡片、边框和文字主题令牌，暗色模式不再保留浅色页面底色和浅色文字。
+ [优化] 精选作品区的内容网格扩大到与首屏一致的宽度，标题、说明和瀑布流左右边缘统一对齐。
+ [变更] 商品画布首页移除“从模板开始”推荐卡片区，保留导入画布、新建项目和我的项目入口。
+ [优化] 首页“三步”步骤区复用使用场景区的列比例并垂直居中，三个步骤卡与上方内容网格对齐。
+ [优化] 首页使用场景、三步和灵感流区统一扩展到同一内容宽度，三列内容与上方作品区保持同轴对齐。
+ [优化] 首页能力带改为贴合首屏的“能力跑道”：压缩高度、左右渐隐、匀速循环，悬停时暂停并突出当前能力。
+ [优化] 首页“三步”区域改为横向流程轨道，增加编号节点、连接线和步骤悬停上浮，减少大块留白。

+ [修复] 画布生图数量达到上限后继续输入不会再把超限值写入配置，积分预估、界面数量与实际生成张数统一按上限计算。

+ [修复] 图片和视频生成任务会持久保存稳定请求编号、原渠道及可恢复结果，生成成功后由后端直存七牛并在刷新后自动接管未完成轮询，避免重复上传、重复扣费或丢失媒体；营销视频页可重新打开生成中记录并在恢复完成后同步预览状态。
+ [优化] 商品图、营销视频、画布、素材库和个人中心的图片与视频下载统一在新标签页打开，避免打断当前工作状态。
+ [优化] 恢复生产静态资源长期缓存，减少首页和顶部导航的批量路由预取，按登录态和后台路由拆分重模块，复用登录后首次工作区快照并压缩导航 Logo，改善首次进入及刷新卡顿。
+ [安全] 开源发布默认配置不再提供公开管理员密码或临时 JWT 密钥；首次空库启动必须显式设置强管理员密码和固定 JWT 密钥，并移除仓库中的生产部署目标与内部工作记忆。
+ [文档] 统一当前仓库、Go module、GHCR 镜像和文档链接，新增上游署名、第三方声明、贡献指南、安全政策、社区准则及 Issue/PR 模板，同时明确保留原项目作者和历次贡献者的版权、许可证与来源说明。
+ [修复] 视频生成任务的前端轮询等待时间延长至 30 分钟，避免较长视频仍在上游生成时被提前标记为失败。
+ [修复] 视频模型扩展能力接入参考视频、参考音频、三类素材合计上限和音频输出；营销视频、画布和模型广场调用文档会按能力组装请求，后端按实际素材及 `generate_audio` 选择可用渠道。
+ [修复] 独立视频生成页会在生成按钮中展示随模型、分辨率和时长同步变化的预计算力消耗。
+ [修复] 480P 视频的近似标准像素尺寸会正确归一化为 16:9 或 9:16，避免渠道规格误判为不支持。
+ [优化] 管理后台渠道模型和模型中心的视频支持时长选项覆盖 4–30 秒全部整数秒。
+ [修复] 图片上游明确建连失败或返回临时服务错误时会安全切换到其他兼容渠道，网关超时优先恢复原任务，故障转移复用原任务和单次预扣；任务状态回写遇到 SQLite 锁竞争时自动退避重试。
+ [修复] 统一图片分辨率的渠道选路、计费和任务日志，VividAI 不再将通用的高质量参数误当成 4K，转发参考图时保留原始 MIME 类型。
+ [修复] 修正非方形图片的 1K/2K/4K 计费档位判断，修复模型中心手工补充能力被渠道配置覆盖、保存后失效，以及图片编辑渠道未填写参考图上限时无法选路的问题。
+ [优化] 管理后台用户列表补充可复制的用户 ID，算力点日志补充用户名，并支持按用户 ID 或用户名检索。
+ [新增] 画布助手支持单轮最多八步的对话式节点创作：复杂任务先展示执行计划，可连续生成图片、视频和文本并排列本轮新节点，生成结果自动记录来源节点并建立画布连线；运行中发送新消息会打断旧任务并立即按新指令继续。
+ [修复] 调整生成记录生命周期：商品图、营销视频、画布和画布助手在请求开始时立即创建“生成中”记录，并在结果返回后更新同一条记录；批量图片逐项更新成功、失败和缩略图，最终区分成功、部分失败与失败。
+ [新增] 新增符合项目主题的模型广场，以搜索、筛选、模型卡片和详情抽屉展示当前模型能力、价格及调用示例，并在顶部主导航和移动端导航提供入口；渠道模型发现支持读取参考图数量与模式，也允许管理员手工覆盖未公布的能力。
+ [优化] 模型详情移除无效的完整接入文档入口；用户 API Key 改为直接永久删除，删除后立即失效且不保留列表记录。
+ [修复] 修复批量媒体上传并发写入 SQLite 时可能导致商品图生成记录整批丢失的问题；商品图、营销视频、画布和画布助手统一串行处理上传凭证写操作，商品图及画布助手对单张结果保存失败保留其余有效记录。

## v0.2.13 - 2026-07-31

+ [新增] 用户 API Key 新增非文本模型列表接口，个人中心快速接入改为模型获取和图片生成示例。
+ [优化] 模型计费取消自动生成及空规格默认价格；图片和视频改为按分辨率精确匹配，视频统一按实际秒数计费，未显式设置价格时直接提示。

## v0.2.12 - 2026-07-31

+ [修复] 用户偏好初始化会先记录默认配置快照，公开模型配置加载和重新部署后的页面刷新不再被误判为用户修改并覆盖已保存的默认模型、张数及音频参数。

## v0.2.11 - 2026-07-31

+ [新增] 个人中心新增企业绑定的用户 API Key，可对外调用 `/api/v1/*` AI 接口并复用现有模型渠道、用户身份、企业算力与计费；密钥仅创建时展示一次，服务端只保存摘要，并支持随时撤销。
+ [安全] 用户 API Key 逐请求复核账号、企业和成员状态，拒绝跨企业伪造，且不能访问账号、后台或企业管理接口。

## v0.2.10 - 2026-07-30

+ [修复] 商品图生成和营销视频页面的工作台记录不再混入画布生成数据，画布图片与视频仍统一保留在个人中心生成记录中。

## v0.2.9 - 2026-07-30

+ [优化] 个人中心资料页改为统一账户概览布局，压缩无效留白并重排基本信息、密码安全和云端空间，用量增加进度表达，长用户编号不再被挤成竖排。

## v0.2.8 - 2026-07-30

+ [修复] 清理生成记录和个人素材表遗留的单用户唯一索引，恢复同一用户多条记录写入；个人中心会从服务端画布补齐已有成功图片和视频，并与正式生成记录去重。
+ [修复] 生图上游返回 `524` 但任务仍在运行时，服务端会按幂等请求编号继续查询并恢复结果，不再让画布提前显示失败或诱发重复生成。
+ [修复] 画布视频节点切换模型时会同步修正该模型不支持的分辨率、比例和时长，参数摘要、设置面板与预计计费不再继续使用旧模型参数。
+ [修复] 模型能力扩展接口兼容视频时长的数字及 `4s`、`10s` 等字符串表示，同时保持默认 `/models` 的严格 OpenAI 响应约束。
+ [修复] 渠道拉取模型后会把本次返回的类型、比例、分辨率和时长同步到新增及已有模型，不再沿用旧能力或跨渠道复用上次发现结果。
+ [修复] 模型中心自动汇总同名启用渠道的类型、操作、比例、分辨率和时长能力，画布与图片、视频生成页会按模型展示实际可用参数，禁用渠道不再影响用户端选项。

## v0.2.7 - 2026-07-29

+ [修复] 移除画布删除项目流程中已废弃的浏览器媒体清理调用，避免画布页面因未定义引用在初始化时直接崩溃。
+ [新增] 画布助手“对话”模式新增服务端 Agent 会话、运行、步骤与可恢复 SSE 事件闭环；开放图片/视频生成、节点排列、文本插入，以及需用户确认的节点删除和文本覆盖工具，均限制在本轮授权选区并基于真实结果继续总结；等待工具超过 15 分钟会安全回收为失败终态。
+ [修复] 画布助手预分配并持久保存会话/运行编号与 SSE 事件游标，刷新后即使运行已终止也会先补放剩余事件；模型步骤和画布工具增加数据库执行租约，图片/视频使用稳定幂等键，所有画布副作用在项目回执同步及 Tool Result 提交完成后才推进游标，避免响应丢失、多实例或多标签页重复规划、生成、扣费和修改画布。
+ [优化] 画布媒体上传改为临时读取元数据后立即释放浏览器资源，并减少平移、拖拽时未变化节点、连线和引用关系的重复计算，改善长时间运行及大型画布的内存和交互性能。
+ [变更] 模型渠道统一为 OpenAI 兼容协议，默认 `/models` 保持严格 OpenAI 格式，`/models?extended=true` 读取 `kind`、支持比例、分辨率和时长能力；视频统一使用 `POST /videos`、`GET /videos/{id}`、`GET /videos/{id}/content`，仅支持单个 `input_reference`，不再改写为 VividAI、火山 Agent Plan、chatgpt2api 或 xAI 私有协议。
+ [优化] 视频模型中心和渠道配置同步比例、分辨率、时长与类型，渠道选路会同时校验操作和视频规格；视频计费支持按“分辨率 × 时长”配置不同的每秒单价。

## v0.2.6 - 2026-07-25

+ [新增] 画布新增“一键电商素材包”，可按商品、SKU 和淘宝、京东、抖音、小红书批量编排并生成平台图片、整套文案与短视频节点。
+ [新增] 企业中心新增共享算力池、个人余额转入、个人/共享扣费模式、月度预算与预警；外部购买取得的兑换码仍充值个人余额，生成任务固化扣费来源并在失败时退回原账户，顶部状态栏显示当前企业的有效余额。
+ [新增] 批量生产 Worker 内置标准图片执行器，复用后台模型渠道、固定电商模板、品牌/商品/SKU 快照和用户组计费规则，支持多参考图、结果归档及失败退款；外部执行器改为可选模式。
+ [新增] 新增 SQLite、MySQL、PostgreSQL 与七牛 Kodo 的停机备份恢复命令，支持版本化清单、文件完整性校验、对象冲突预检和脱敏结构化日志。
+ [新增] 企业批量生产补齐渠道交付规格、结果质量规则核对和 2–4 项并排选片，审核人可结合任务快照、品牌规范、禁用词和 SKU 参考图完成审核及主图选择。
+ [新增] 管理后台新增只读数据巡检及三类安全修复动作，覆盖媒体引用、对象存储、生成记录、批量结果和算力账本一致性。
+ [优化] 移动画布新增双指中心缩放、节点长按菜单与无键盘多选入口，并调整窄屏工具栏、缩放控件、小地图和助手面板布局。

+ [工程] 新增 CI 质量关卡及企业隔离、会话恢复、Cookie 安全属性、媒体上传配额/回收边界和批量 Worker 租约/幂等自动化测试。
+ [工程] 新增画布保存版本冲突、幂等重放、历史裁剪、跨企业媒体引用和事务回滚自动化测试。
+ [新增] 新增请求 ID、JSON 访问日志、后台 Worker 结构化脱敏日志、liveness、数据库 readiness 及管理员队列/Outbox 健康摘要，并补充最近 24 小时生成成功率、平均/P95 耗时和可配置积压阈值告警。
+ [修复] 生成任务、余额预扣和消费流水改为事务写入，并通过请求编号和任务终态保证扣费、失败退款幂等，避免任务写入失败仍扣费或重复请求、重复回调造成余额异常。

+ [修复] 商品图生成页和画布生图按模型能力显示参考图与质量设置，不再向不支持的模型发送遗留参考图或质量值。
+ [修复] 视频生成页和画布视频按模型能力显示参考图、参考视频和参考音频，并过滤当前模型不支持的参考素材。
+ [修复] VividAI 模型渠道同步聚合接口协议，并按各原厂模型的编辑能力、视频时长、比例和清晰度约束请求参数。
+ [修复] 图片、视频、音频任务及参考素材请求补齐当前企业上下文，避免生成时提示“缺少企业上下文”。
+ [优化] 生图工作台的历史缩略图和结果卡片改用七牛私有 WebP 变体并启用懒加载，放大与下载保留原图，复用已存图片时不再重复下载上传。
+ [修复] 旧账号升级到企业工作区后自动补齐历史画布、素材、生成记录、媒体文件和生成任务的企业归属，避免数据仍在但页面不可见。
+ [新增] 新增标准企业多租户、成员邀请与角色权限、企业切换、审计日志、品牌中心、商品/SPU/SKU 中心及持久化批量生产任务队列。
+ [安全] 画布、商品素材、生成记录、媒体文件、对象存储路径和模型任务改为企业级隔离，后端按当前企业成员关系和角色强制鉴权。
+ [修复] 企业上下文改为请求级校验，补齐注册与默认企业事务、邀请和所有权并发保护、批量任务租约与取消终态、服务端分页及商品引用删除保护。
+ [修复] 企业工作区改用复合主键和乐观版本控制，网络异常与离线编辑不再清除未保存数据，邀请过期、引用锁和事务型审计补齐。
+ [修复] 企业媒体上传新增并发配额预留、唯一临时对象键、事务型文件引用、24 小时延迟回收及带租约的对象删除 Outbox，避免并发超额、替换覆盖和保存/回收竞态。
+ [修复] 企业邀请邮件改用事务型 Outbox、租约领取和退避重试；批量生产输入改为品牌/商品/SKU 去重快照并限制单任务快照总量为 16MB。
+ [优化] 企业中心列表按当前标签加载，批量任务仅在当前页存在运行中项目时轮询。
+ [修复] 上传确认新增配额二次校验，GC 会保护有效预留；七牛对象删除彻底移出用户请求并只由租约 Worker 消费。
+ [安全] 品牌 Logo、SKU 参考图、批量输入媒体和批量产出统一改用企业私有 `storageKey` 与事务型文件引用，Worker 会把执行器结果归档到企业对象存储。
+ [修复] 会话恢复忽略失效的租户 Header，并将工作区状态统计改为数据库计数，避免成员移除后无法恢复及重复加载完整业务 JSON。
+ [修复] 原生媒体 URL 显式携带企业上下文并逐请求鉴权，修复图片、视频、音频及批量结果无法附带租户 Header 的问题。
+ [安全] 批量结果归档改为拒绝私网目标、限制重定向和实际读取大小的可取消流式上传，并在归档前同步确认任务租约。
+ [修复] SKU 参考图在上传前按已保存数量和进行中数量限制最多 50 张，避免超限文件先占用企业额度。
+ [修复] 媒体维护任务可主动发现新失去引用的文件，上传凭证与确认结果强制匹配存储编号的媒体类型；品牌、商品和 SKU 表单重置旧值，并保护异步上传生命周期。
+ [修复] 品牌、SPU、SKU 增加乐观版本冲突保护，批量任务创建增加企业内请求幂等键；媒体无引用时间改为业务事务内维护，直传支持超时和取消，并修复素材库视频存储编号。
+ [修复] 品牌、SPU、SKU 删除补齐版本条件和当前时间回收标记；批量任务幂等键绑定请求摘要，Worker 结果归档可在租约重投递时复用。
+ [修复] SKU 变更同步推进父商品版本，批量任务改为在企业锁事务内读取并展开 SKU，避免旧商品级联误删和并发任务漏产。
+ [安全] 企业媒体以七牛 ETag 固化 `storageKey` 内容身份，冲突上传会事务清理；批量结果使用任务随机令牌和保留前缀隔离内部命名空间。
+ [修复] 企业媒体上传统一为 20 分钟凭证与预留，取消时即时释放额度并延迟清理在途对象；HEAD 检查补齐 ETag，企业设置和成员管理补齐乐观版本条件。
+ [新增] 新增可独立部署的批量生产 Worker，通过输入快照、幂等执行器、统一租约锁序完成任务领取、续租、超时失败和安全结果回写。
+ [安全] 企业临时对象新增独立字节账本和物理空间上限，取消、替换或过期的上传凭证不能绕过企业额度持续制造待清理对象。
+ [修复] 批量队列改为按企业游标公平轮转并限制单企业最多 10000 个待处理项；Worker 强制 HTTPS、禁止鉴权请求重定向并脱敏任务错误信息。
+ [安全] 七牛私有下载域名强制 HTTPS；企业媒体新增正式文件、活动预留和待清理对象数量上限，小文件不能绕过字节额度持续放大数据库记录。
+ [优化] 批量领取移除全局任务聚合并补充复合索引，新增单企业运行并发限制；外部执行和归档错误日志只记录错误类型。
+ [安全] 上传凭证接口增加跨实例一致的企业/用户分钟窗口限流，防止高频小文件请求持续消耗数据库和对象存储控制面。
+ [调整] 用户端收敛为电商视觉工作台，新增商品主图、场景图、详情页、促销图、服饰模特图和 SKU 系列图任务模板，并调整首页、商品画布、素材与营销视频入口。
+ [新增] 新增账号级用户偏好同步，主题、默认模型、生成参数和图片快捷工具栏只保存到服务端并可在登录设备间自动恢复。
+ [优化] 用户业务数据改为纯云端存储：浏览器不再持久化画布、素材、生成记录、媒体、账号偏好或离线队列，不保留任何旧缓存兼容逻辑。
+ [安全] 用户端移除自定义 Base URL、API Key 和本地直连渠道，AI 请求统一通过同源后端，并改用 HttpOnly Cookie 恢复会话。
+ [修复] 生图和视频生成记录改为账号级云端保存，并可在登录设备之间恢复。
+ [优化] 账号图片、视频和音频改用七牛云 Kodo 私有空间保存，通过短期凭证直传并由后端鉴权读取。
+ [新增] 用户端新增个人中心，支持个人资料与密码维护、账号图片/视频生成记录汇总及本人算力明细。
+ [修复] `chatgpt2api` 图片生成改用异步任务轮询和连接保活，避免上游完成但前台提前显示失败。
+ [修复] 修复 AI 上游连接中断时生成失败详情显示问号或乱码的问题。
+ [新增] 注册强制使用 SMTP 邮箱验证码，并支持用户名或邮箱加密码登录及后台邮箱域名限制。
+ [优化] 管理后台新增统一模型中心，通过模型能力注册表识别生成/编辑端点，并支持为不同分辨率分别定价。
+ [修复] 多渠道同名图片和视频模型会按操作及分辨率能力选择上游，避免高规格请求进入能力不足的渠道。
+ [新增] 新增平台公告、每日签到奖励和新用户注册赠送额度后台配置。
+ [优化] 前台隐藏版本号并移除 Linux.do 登录流程。
+ [优化] 国内访问限制改为系统设置开关，关闭时放行，开启后才拦截中国大陆请求。
+ [优化] 管理后台系统设置按模型、访问、运营、邮件和同步拆分为分类标签页，模型渠道迁移为独立后台菜单页面。
+ [修复] 修复暗色主题下 Tooltip 提示文字对比度不足的问题。

## v0.2.5 - 2026-06-05

+ [新增] 顶部用户状态栏显示登录用户算力点余额，并支持前台实时刷新。
+ [修复] 修正 4K 图像尺寸预设，避免部分比例超过图像总像素上限导致生成失败。

## v0.2.4 - 2026-06-04

+ [新增] 新增图片反推提示词功能。

## v0.2.3 - 2026-06-04

+ [新增] 新增图片蒙版局部修改功能。
+ [优化] 优化配置节点@图片功能。

## v0.2.2 - 2026-06-04

+ [新增] 新增图片放大工具。
+ [优化] 优化图片工具条，增加自定义功能。
+ [修复] 修复端口冲突问题、pg/mysql未初始化问题。

## v0.2.1 - 2026-06-03

+ [新增] 新增文档站点页面。
+ [优化] 优化画布连线交互。
+ [优化] 优化模型选择用户偏好。

## v0.2.0 - 2026-06-01

+ [新增] 支持通过火山方舟AgentPlan接入。
+ [新增] 视频生成支持声音、水印及图片/视频/音频参考输入。
+ [新增] 画布新增音频节点。
+ [优化] 图片/视频素材支持 `图片1`编号注入提示词。

## v0.1.1 - 2026-05-30

+ [新增] 支持New API跳转并自动填入Base URL和API Key配置。

## v0.1.0 - 2026-05-26

+ [优化] 优化我的画布、我的素材导出功能
+ [修复] 修复画布撤销，配置节点等bug问题

## v0.0.9 - 2026-05-26

+ [新增] 新增视频创作台页面。
+ [修复] 修复图片节点size参数传递问题。

## v0.0.8 - 2026-05-24

+ [新增] 新增用户账号与算力点体系，支持账号密码注册登录、Linux.do OAuth。
+ [新增] 管理后台公开配置支持设置模型算力点、支持计费查询。
+ [新增] 画布右上角展示用户算力点余额，生成按钮会展示本次预计消耗算力点。
+ [新增] 新增视频生成节点。

## v0.0.7 - 2026-05-23

+ [新增] 管理后台提示词管理支持多选批量删除。
+ [新增] 新增定义拉取GitHub提示词源功能。
+ [新增] 新增awesome-gpt-image2-prompts提示词来源。
+ [优化] 优化模型下拉选择样式、优化生图编辑设置

## v0.0.6 - 2026-05-22

+ [新增] 管理后台支持配置模型渠道，前端当前无需鉴权即可直接使用后端渠道能力。
+ [优化] 统一整理后端错误提示、AI 代理、图片节点生成与重试、参考图缺失处理等细节。
+ [优化] 后端模型代理路径调整为 OpenAI 风格。

## v0.0.5 - 2026-05-20

+ [新增] 右上角版本号支持点击查看版本更新弹窗，展示当前版本、最新版本和按时间线整理的更新日志。
+ [新增] 设置弹窗支持配置系统提示词，AI 生图、编辑图和文本请求会自动携带。

## v0.0.4 - 2026-05-20

+ [调整] Docker 运行入口改为 Next.js 对外提供页面，`/api/*` 由 Next.js 代理到内部 Go 服务。
+ [修复] 文本复制在局域网 IP 访问时可能失败的问题。

## v0.0.3 - 2026-05-19

+ [修复] 更新 nanoid 依赖并修改 ID 生成方式，防止其他ip无法使用crypto模块导致的ID生成失败问题。

## v0.0.2 - 2026-05-19

+ [新增] 增加生图工作台功能，支持文生图、图生图、查看历史记录，并增加移动端适配。
+ [修复] 画布生成尺寸控件支持选择更多常用比例，并可直接输入自定义比例。
+ [修复] 生成配置节点恢复拖拽操作，避免面板控件拦截整块节点拖动。
+ [文档] 增加 Render 部署说明。

## v0.0.1 - 2026-05-19

+ [新增] 首次开源版本，包含无限画布能力：多画布项目、节点拖拽缩放、连线、小地图、撤销重做、导入导出。
+ [新增] AI 创作能力：支持 OpenAI 兼容接口的文生图、图生图、参考图编辑和文本问答。
+ [新增] 画布助手能力：支持围绕选中节点和上游节点对话、生图，并把结果插回画布。
+ [新增] 提示词库能力：抓取多个 GitHub 开源项目，按案例整理数百个图片提示词。
